[package]
name = "cachebox-protocol"
version = "0.1.0"
edition = "2024"
description = "Native protocol types and codec for Cachebox."
license = "MIT"
repository = "https://github.com/smarzola/cachebox"

[dependencies]

[dev-dependencies]
serde_json = "1.0.150"
